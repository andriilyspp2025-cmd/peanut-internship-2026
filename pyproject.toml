[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "peanut-internship-2026"
version = "0.1.0"
description = "CEX-DEX crypto arbitrage bot"
authors = [{name = "Author"}]
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]